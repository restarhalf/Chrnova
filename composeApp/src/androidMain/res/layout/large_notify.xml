<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingHorizontal="0dp"
    android:paddingVertical="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:layout_toStartOf="@+id/btn_mute_large"
            android:orientation="vertical">

            <TextView
                android:id="@+id/notification_title"
                style="@style/TextAppearance.Compat.Notification.Title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:text="这是一个非常重要的日程提醒"
                android:textColor="@color/widget_title_text"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/notification_body"
                style="@style/TextAppearance.Compat.Notification.Line2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:lineSpacingExtra="4dp"
                android:maxLines="4"
                android:text="详细的日程内容写在这里。MIUIX的设计语言通常留白较多，文字层级分明，视觉重点突出。圆润的边角和统一的柔和色调是核心特征。高度自适应以支持更多的文本内容展示。"
                android:textColor="@color/widget_subtitle_text"
                android:textSize="12sp" />

        </LinearLayout>

        <TextView
            android:id="@+id/btn_mute_large"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="16dp"
            android:background="@drawable/btn_mute_bg"
            android:gravity="center"
            android:paddingHorizontal="16dp"
            android:text="上课静音"
            android:textColor="@color/widget_dot"
            android:textSize="13sp"
            android:textStyle="bold" />

    </RelativeLayout>

</LinearLayout>